# Shared Docksal configuration, stored in the repo.
# Local overrides go in .docksal/docksal-local.env (gitignored).

# No stack: services are defined in .docksal/docksal.yml.
# msgpit serves itself on port 8080, so there is no web container.
DOCKSAL_STACK=

CLI_IMAGE=docksal/cli:php8.3-3.9
